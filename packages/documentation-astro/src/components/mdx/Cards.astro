---
const { className, ...props } = Astro.props;
---

<div {...props} class:list={["cards", className]}><slot /></div>

<style>
  .cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
    gap: 1rem;
    margin: 1.5rem 0;
  }
</style>
